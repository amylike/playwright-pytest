import pytest


# @pytest.fixture(scope="session", autouse=True)
# def main_url(pytestconfig):
#     """테스트를 수행할 테스트 환경의 url의 입력을 받아 설정"""
#     main_url = pytestconfig.getini("main_url")
#     return main_url
